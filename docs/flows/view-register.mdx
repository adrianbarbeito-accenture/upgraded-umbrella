---
title: Register
description: "User flow in the `view` feature."
---

## Sequence

```mermaid
sequenceDiagram
  participant U as User
  participant S as RegisterScreen
  participant C as RegisterCubit
  participant R as Repository
  participant API as Backend
  participant DB as MongoDB

  U->>S: interact
  S->>C: dispatch
  C->>R: call method
  R->>API: POST /v1/account/register
  API->>DB: query
  DB-->>API: result
  API-->>R: 201 response
  R-->>C: data
  C-->>S: emit state
  S-->>U: render
```

## Narrative

> TODO (flow prose): run `npm run generate` with `ANTHROPIC_API_KEY` set to fill this in.

## Components

- Screen: [`RegisterScreen`](/mobile/screens/register-screen)
- Cubit: [`RegisterCubit`](/mobile/state/register-cubit)
- Repository: _unknown_

## Endpoints

- [`POST /v1/account/register`](/api/account/account-controller-register)
