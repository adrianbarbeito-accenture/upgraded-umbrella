---
title: PUT /v1/account/password/forgot-update
description: Update password for the account code provided belongs.
---

**Tag:** `Account` &nbsp;·&nbsp; **Operation:** `AccountController_passwordFotgotUpdate`

## Purpose

> TODO (endpoint prose): run `npm run generate` with `ANTHROPIC_API_KEY` set to fill this in.

## Sequence

```mermaid
sequenceDiagram
  participant C as ResetPasswordScreen
  participant API as Backend
  participant DB as MongoDB
  C->>API: PUT /v1/account/password/forgot-update
  API->>DB: query
  DB-->>API: result
  API-->>C: 204
```

## Parameters

| Name | In | Required | Type | Description |
| ---- | -- | -------- | ---- | ----------- |
| `Content-Type` | header | no | `string` | application/json |

## Request body

Schema: [`UpdateForgotPasswordDto`](/data-model/update-forgot-password-dto)

| Property | Type | Required | Description |
| -------- | ---- | -------- | ----------- |
| `password` | `string` | yes |  |
| `forgotPasswordCode` | `string` | yes |  |

## Responses

- **204** — ForgotPasswordCode used to update password
- **400** — Some inputs are missed or wrong
- **429** — Too many requests, rate limit exceeded
- **500** — Error not handled

## Used by

- [`ResetPasswordScreen`](/mobile/screens/reset-password-screen)
