---
title: POST /v1/account/password/forgot-init
description: Allow users to send an email with a code to request the change of a forgotten password.
---

**Tag:** `Account` &nbsp;·&nbsp; **Operation:** `AccountController_forgotPassword`

## Purpose

Initiates the forgotten-password flow by emailing a one-time code to the address provided. The call is unauthenticated by design and is the entry point of the recover-password screen. A success only confirms that the email was queued — it does not reveal whether the address corresponds to a real account.

## Sequence

```mermaid
sequenceDiagram
  participant C as RecoverPasswordScreen
  participant API as Backend
  participant DB as MongoDB
  C->>API: POST /v1/account/password/forgot-init
  API->>DB: query
  DB-->>API: result
  API-->>C: 204
```

## Parameters

| Name | In | Required | Type | Description |
| ---- | -- | -------- | ---- | ----------- |
| `Content-Type` | header | no | `string` | application/json |

## Request body

Schema: [`EmailDto`](/data-model/email-dto)

| Property | Type | Required | Description |
| -------- | ---- | -------- | ----------- |
| `email` | `string` | yes |  |

## Responses

- **204** — The email for forgotten password has been sent successfully
- **400** — Some inputs are missed or wrong
- **429** — Too many requests, rate limit exceeded
- **500** — Error not handled

## Used by

- [`RecoverPasswordScreen`](/mobile/screens/recover-password-screen)
